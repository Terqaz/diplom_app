{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container mx-auto" style="max-width:550px;">
        <a href="{{ path('app_user_show', {'id': user.id}) }}" class="btn btn-outline-secondary mb-3">
            Назад </a>
        <h3 class="mb-2 text-center">Редактировать персональные данные</h3>

        {{ include('user/_form.html.twig', {'button_label': 'Сохранить'}) }}
    </div>
{% endblock %}
